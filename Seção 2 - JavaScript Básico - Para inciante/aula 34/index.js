for(let i = 1; i < 11; i++){
    const par = i % 2 == 0 ? 'Par': 'Ímpa';
    console.log(`Linha ${i}, número é ${par}.`)
}

const frutas = ['Marça', 'Pêra', 'Uva','Melância','Kiwi'];
for (let i = 0; i < frutas.length; i++) {
    const fruta = frutas[i];
    console.log(`Fruta: ${fruta}`);
}
